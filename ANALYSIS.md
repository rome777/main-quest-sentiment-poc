# 결과 분석

숫자 표와 혼동행렬, 오분류 20건 전체 목록은 [`results/comparison.md`](results/comparison.md)
(자동 생성)에 있다. 여기는 그 숫자를 어떻게 읽어야 하는지를 적는다.

> **먼저 읽을 것 — 이 표의 숫자는 "정확도"가 아니다.** 참조 라벨을 사람이
> 아니라 Claude 가 매겼고(사람 검수 없음), 그마저 Gemini 라벨을 본 상태에서
> 매겼다. 아래 모든 숫자는 **LLM 참조 라벨과의 일치율**이다. 자세한 것은
> [한계](#한계) 절 — 이 PoC 에서 가장 중요한 부분이다.

검증은 두 차례로 나눠 진행했다. **1차**는 [PROBLEM.md](PROBLEM.md)에 모델을
돌리기 전 사전 등록한 후보 3개다. **2차**는 1차 결과를 보고 "사전학습 말뭉치를
바꾸면 달라지나"를 확인하려 파인튜닝 후보 3개를 추가한 것이다. 검증셋 20건과
성공 기준(70% 이상 + 방향 반전 2건 이하)은 두 차례 모두 동일하며, 2차 후보는
결과를 본 뒤 추가했으므로 사전 등록 후보와 같은 무게로 읽으면 안 된다.

## 결론: 파인튜닝 후보 네 개가 모두 기준 미달, 채택하지 않는다

| 후보 | 일치율 | 방향 반전 |
|---|---|---|
| Gemini 2.5 Flash API (기준선) | 65% | 0건 |
| KcELECTRA-base-v2022 | 60% | 3건 |
| KR-FinBert-SC | 55% | 3건 |
| KoELECTRA-v3 | 45% | 6건 |
| klue/roberta-base | 40% | 6건 |
| 규칙 기반 (하한선) | 35% | 0건 |

네 개 전부 일치율 70% 와 방향 반전 2건을 동시에 못 넘었다. "지금 방식이 낫다"도
유효한 PoC 결과다. 다만 아래 **한계** 절의 정답 라벨 문제 때문에, 이 숫자를
"파인튜닝이 안 된다"의 근거로 쓰기 전에 검증셋부터 다시 만들어야 한다.

## 1차 — 사전 등록한 후보 3개

- 3건의 방향 반전(#4, #5, #7)은 모두 **비속어·거친 어조가 낀 문장**이다.
  `아래에서 탄 새끼들 지금 좆도 안쫄리면서 ㅅㅂ`(자신감 있는 긍정)을 부정으로,
  `스토리지 잘가세요~~`(조롱조 작별=부정)를 긍정으로 뒤집었다. 학습 데이터가
  Gemini 라벨을 그대로 베낀 실버 라벨 2,676건뿐이고 사람이 다시 검수하지
  않아서, 모델이 "비속어 = 감정 강도" 정도만 배우고 그 비속어가 향하는
  대상(시장이냐 사람이냐, 긍정적 맥락이냐 조롱이냐)까지는 못 잡아낸 것으로
  보인다.
- 반대로 파인튜닝 모델이 Gemini보다 잘 잡은 것도 있다. #13(매매할게 없다),
  #17(기분좋게 점심먹으려하는데)처럼 정보가 부족해 NEUTRAL이 맞는 사례에서
  Gemini는 섣불리 방향을 단정했지만 파인튜닝 모델은 정확히 맞혔다.
- 규칙 기반(35%)은 그대로 "확인용 기준선" 역할을 했다. 코드에 박힌 키워드
  목록(가즈아·떡상·한강·폭락 등)이 이 커뮤니티가 실제로 쓰는 말(숏·청산·
  포모·개추·설거지)과 거의 안 겹쳐서 20건 중 18건을 그냥 NEUTRAL로 찍었다.

## 2차 확장 — 사전학습 말뭉치를 바꾼 3개

1차에서 KcELECTRA가 일치율은 Gemini에 근접(60% vs 65%)하는데 방향 반전만
3건 났다. 이게 **파인튜닝 방식의 한계**인지 **그 모델 말뭉치의 한계**인지
가리려고, 문체와 도메인이 다른 사전학습 모델 3개에 같은 학습·평가를 돌렸다.

| 모델 | 사전학습 말뭉치 | 일치율 | 방향 반전 |
|---|---|---|---|
| KcELECTRA-base-v2022 | 네이버 뉴스 댓글 (구어체) | 60% | 3건 |
| KR-FinBert-SC | 경제뉴스·증권사 리포트 (금융·격식체) | 55% | 3건 |
| KoELECTRA-v3 | 뉴스·위키·블로그 (격식체) | 45% | 6건 |
| klue/roberta-base | 위키·뉴스 (격식체) | 40% | 6건 |

**말뭉치가 대상 텍스트에서 멀어질수록 방향 반전이 정확히 두 배로 늘었다.**
구어체(KcELECTRA) 또는 금융 도메인(KR-FinBert) 중 하나라도 맞은 모델은 3건,
문체·도메인이 둘 다 어긋난 모델(KoELECTRA, klue/roberta)은 6건이다.
"도메인 특화 모델을 고르면 출발선이 가깝다"는 [PROBLEM.md](PROBLEM.md)의
가설 자체는 방향이 맞았다 — 다만 그 이득이 기준선을 넘을 만큼 크지 않았다.

반전이 난 항목을 보면 어긋난 축이 무엇인지가 드러난다.

- **#4(`푸틴형 미사일싸줘요 빵야빵야`), #7(`아래에서 탄 새끼들...ㅅㅂ`)은 네 모델
  전부 뒤집었다.** 은유와 비속어가 향하는 대상을 읽어야 하는 사례로, 말뭉치를
  바꿔서 해결되는 문제가 아니다. 실버 라벨의 한계로 봐야 한다.
- **#11(`억단위 익절 얼마만이노 ㅋㅋㅋㅋㅋ`)** 은 KcELECTRA만 맞히고 나머지
  셋이 부정으로 뒤집었다. KR-FinBert는 긍정을 6건 중 2건밖에 못 잡는데
  (혼동행렬 참고), 경제 뉴스·리포트의 절제된 문체로 사전학습된 모델이
  커뮤니티의 과장된 환호를 밋밋하게 읽는 것으로 보인다.
- 대신 **KR-FinBert는 중립을 7건 중 5건으로 가장 잘 잡았다.** Gemini(2건)·
  KcELECTRA(3건)보다 낫다. 격식체 말뭉치가 "판단을 유보하는 문장"에는
  오히려 유리하게 작용했다. 성격이 다른 두 모델이라 앙상블 여지는 있지만,
  이번 기준을 넘기기에는 둘 다 방향 반전이 3건씩이라 의미가 없다.
- **KR-FinBert는 라이선스가 모델 카드 어디에도 없다**(2026-09-22, HuggingFace
  README·태그 확인). 성능과 무관하게 상용에는 이 후보를 쓸 수 없다.

## 뜻밖의 발견 — 지금 쓰는 방식(Gemini)의 신뢰성 문제

검증하다 기준선 쪽에서 문제가 둘 나왔다. 둘 다 이 PoC 범위 밖이지만
**PoC 결과와 무관하게 지금 고칠 값어치가 있다.**

**1. 파싱 실패를 재시도하지 않는다.** Gemini API가 20건 중 2건(#4·#19 — 둘 다
반어법이 낀 "어려운" 사례)에서 5번을 재시도해도 계속 JSON이 중간에 끊겨
파싱에 실패했다. 프로덕션의 재시도 로직은 429/5xx만 재시도 대상으로 보고 이런
파싱 실패는 재시도하지 않는다 — 반어법이 섞인, 감성 신호가 가장 중요한 글에서
조용히 잡이 실패하고 있을 가능성이 있다.

**2. 같은 글에 같은 모델이 다른 답을 낸다.** 검증셋에는 운영 DB에 저장돼 있던
Gemini 라벨(`gemini_label`)이 함께 들어 있는데, 이번 PoC에서 같은 20건을 다시
호출한 결과와 **4건이 달랐다.**

| idx | 저장된 운영 라벨 | 이번 재호출 | 정답 |
|---|---|---|---|
| #4 | NEGATIVE | 파싱 실패 | NEGATIVE |
| #13 | NEGATIVE | NEUTRAL | NEGATIVE |
| #17 | NEGATIVE | POSITIVE | NEUTRAL |
| #19 | NEGATIVE | 파싱 실패 | NEUTRAL |

그래서 **Gemini의 점수 자체가 언제 재느냐에 따라 흔들린다.** 저장된 운영
라벨로 채점하면 15/20(75%)이고, 이번 재호출로 채점하면 13/20(65%)이다.
20건짜리 검증셋에서 합격선이 70%인데 기준선이 ±10%p 로 움직인다는 뜻이라,
**지금 검증셋 크기로는 60%와 65%의 차이를 유의미하게 해석할 수 없다.**
1차·2차의 모든 순위 비교는 이 점을 감안하고 읽어야 한다.

## 한계

**가장 큰 문제는 참조 라벨이다. 사람이 매긴 것이 아니다.**
`poc/data/eval_set.csv` 의 `reference_label` 칸은 **Claude 가 매긴
것이고, 사람 검수를 거치지 않았다.** PROBLEM.md 에 "사람이 직접 판단했다"고
적혀 있던 것은 사실이 아니어서 [2026-09-22 정정했다](PROBLEM.md).

매긴 주체는 **Claude Sonnet 5 로 보인다** — 검증셋을 넣은 커밋 `64fdf41` 의
`Co-Authored-By` 트레일러가 그렇게 적혀 있다. **어떤 노력(effort) 설정이었는지는
어디에도 기록이 없다.** 같은 커밋 메시지 본문에도 "사람이 직접 정답 확정"
이라는 같은 허위 진술이 들어 있어서, git 이력만 봐도 라벨 출처를 잘못 알게 된다.

여기서 두 가지가 따라온다.

**1. 이 PoC 가 재는 것은 정확도가 아니라 LLM 사이의 일치율이다.** 채점자가
사람이 아니므로 "무엇이 맞는 답인가"에 대한 독립적 근거가 없다. 표의 모든
숫자는 "Claude 가 매긴 라벨과 얼마나 같은 답을 내느냐"로 읽어야 한다.

**2. 그 참조 라벨마저 Gemini에 앵커링돼 있다.** `eval_set.csv` 는 `gemini_label`
바로 옆 칸에 `reference_label` 을 적는 구조라, 라벨을 매길 때 Gemini의 답이 같은
화면에 있었다. 결과적으로 **20건 중 15건이 Gemini 라벨과 같다.** 기준선 후보가
자기 답을 보고 만든 기준으로 채점받는 구조다. Gemini의 65~75% 중 얼마만큼이
실력이고 얼마만큼이 앵커링인지 이 데이터로는 분리할 수 없다.

게다가 파인튜닝 모델들은 **Gemini 실버 라벨로 학습해놓고 Claude 라벨로
채점받았다.** 두 라벨 소스가 어긋나는 지점(20건 중 5건)에서 모델은 배운 대로
답하고 틀린 것으로 기록된다. 구조적으로 불리한 위치다. 40~60% 라는 폭은 이
confound 를 걷어내기 전까지 모델 성능으로 단정하면 안 된다.

그 밖에:

- 검증셋이 20건뿐이다 — 크기부터 결론을 지탱하지 못한다.
- 학습 자료가 사람 검수 라벨이 아니라 Gemini가 매긴 실버 라벨 2,676건이다.
- `raw_post.body` 가 항상 비어 있어 제목 한 줄만으로 분류한다.
- 4chan·Reddit(영어)은 범위 밖이다.
- 2차 후보 3개는 1차 결과를 본 뒤 고른 것이라 사전 등록 후보가 아니다.

## 다음 단계

1. **정답 라벨부터 다시 만든다.** Gemini 라벨을 가린 상태에서, 가능하면 두 명
   이상이 독립적으로 매기고 불일치만 조정한다. 검증셋도 20건에서 최소 100건대로
   늘려야 65% 와 60% 의 차이를 말할 수 있다. 이게 해결되기 전의 순위는 잠정이다.
2. 학습 자료 중 표본만이라도 사람이 직접 검수한 라벨로 바꾼다.
3. `UNFROZEN_LAYERS`를 늘리거나 학습 데이터를 키워 방향 반전 오류부터 줄인다.
4. Gemini의 JSON 파싱 실패를 재시도 대상에 추가한다(PoC 결과와 무관하게
   지금 고칠 가치가 있는 버그).
5. 데이터가 늘어나면 임계값으로 "확신 없음"을 걸러 사람 검수로 넘기는
   선택지도 있다.
