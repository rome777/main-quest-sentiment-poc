# 문제 정의서 — 커뮤니티 글 감성분류, LLM 외부 API 탈출

작성일: 2026-09-22

## 도메인 설명

[han-river-view-or-dive](https://github.com/rome777/han-river-view-or-dive) 는
디시인사이드·코인판·레딧·4chan 에서 암호화폐 관련 글을 실시간으로 수집해
감성(POSITIVE/NEGATIVE/NEUTRAL)을 분류하고, 그 분류 결과를 시그널 엔진의
입력(`sentiment_bucket`)으로 써서 매매 여부를 판단하는 개인 자동매매 시스템이다.

지금까지 쌓인 원문 글은 `raw_post` 테이블에 **167,801건**, 그중 감성 라벨이 붙은
글은 `post_sentiment` 테이블에 **126,506건**이다(2026-09-22 기준, 로컬 개발 DB 실측).
글은 제목만 있고 본문은 항상 비어 있다(백필 단계 설계 — 크롤러가 본문을
수집하지 않는다).

## 현재의 문제

지금 분류를 맡고 있는 것은 **Google Gemini 2.5 Flash API**
(`src/hanriver/llm/client.py` `GeminiClassifier`)다. 시스템 프롬프트에
"암호화폐 커뮤니티 글의 감성분류"를 지시하고, 구조화 출력 스키마로
`{label, confidence, intensity, is_sarcasm}` 를 강제해서 받는다.

이 방식의 문제는 셋이다.

1. **자료가 외부로 나간다.** 글 원문(제목)이 매 건 구글 서버로 전송된다.
   지금은 익명 커뮤니티 공개 글이라 개인정보 문제는 크지 않지만, 뉴스 채널이나
   비공개 소스가 추가되면 이 경로가 바로 걸린다.
2. **건당 과금이다.** 잡 로그 기준 크롤·분류 잡이 하루 수십~수백 회 돈다
   (`job_classify_*` 이벤트 2,030회, 최근 로그 기준). 분류 대상이 늘수록
   비용이 선형으로 늘고, API 쿼터·요금 정책 변경에 그대로 노출된다.
3. **오프라인에서 못 돈다.** 네트워크·API 키·요금제 중 하나라도 막히면
   분류 파이프라인 전체가 멈춘다. `create_classifier()` 에 이미
   `MockClassifier`(키워드 매칭 폴백)가 있다는 사실 자체가, 이 리스크를
   저자가 이미 알고 있었다는 뜻이다.

## 개선 가설

이 업무는 강의 2강의 **"분류와 배분"** 유형이다. 병목은 판단 자체가 아니라
판단의 총량(하루 수천 건)이다. 판단 기준(POSITIVE/NEGATIVE/NEUTRAL 세 갈래)은
이미 프롬프트로 명문화되어 있고, 12만 건이 넘는 과거 판정 기록(Gemini 라벨)이
있어 채점 자료를 만들 수 있으며, 오분류가 나도 사람 목숨이나 법적 효력에
영향을 주지 않고(페이퍼 트레이딩 우선 운용 중) 되돌릴 수 있다. 발생량도
충분하다. 강의 6강의 네 조건(판정 기준·채점 자료·되돌릴 수 있음·발생량)을
모두 만족하는 지점이다.

가설: **한국어 커뮤니티 특유의 구어체·은어(가즈아, 한강, 설거지, 개추 등)로
사전학습된 도메인 특화 모델을 우리 데이터로 파인튜닝하면, 이 3-분류 문제에서
Gemini API와 견줄 만한 정확도를 로컬·무료·오프라인으로 낼 수 있다.**
범용 LLM을 그대로 쓰는 대신, 강의 5강의 파인튜닝 기법(사전학습된 모델의
앞쪽 층은 얼리고 뒤쪽 판단층만 우리 데이터로 다시 학습)을 적용한다.

## 대상 사용자

이 시스템의 유일한 사용자 — 즉 나 자신. 시그널 엔진이 이 분류 결과를 그대로
매매 판단에 쓰므로 "사용자"는 사람이 아니라 하위 파이프라인(`sentiment_bucket`
계산 잡)이다.

## 성공 기준 (모델을 돌리기 전에 확정)

한국어 게시글(디시인사이드·코인판) 20건으로 검증셋을 만들고, **사람이
Gemini 라벨을 참고는 하되 최종 판단은 직접 다시 내려 정답을 미리 확정**했다
([`poc/data/eval_set.csv`](poc/data/eval_set.csv), 20건 중 8건은 반어법·정보
부족 등으로 Gemini와 판단이 갈리거나 애매한 "어려운" 사례로 골랐다).

- **주 기준**: 이 20건에서 사람 라벨과 **14건(70%) 이상** 일치하면 로컬
  대체 후보로 채택할 가치가 있다고 본다. (참고: 아무 판단도 안 하고 전부
  NEUTRAL로 찍는 기준선은 20건 중 7건 = 35% 다.)
- **치명 오류 제한**: POSITIVE↔NEGATIVE 로 방향이 정반대로 뒤집히는
  오류가 **2건을 넘으면 기준을 만족해도 채택하지 않는다.** 이 시스템은
  분류 결과로 매수/매도를 결정하므로, 방향이 뒤집히는 오류가 NEUTRAL
  오분류보다 훨씬 비싸다.
- **참고 지표(합격/불합격을 가르지는 않음)**: Gemini API 대비 정확도 차이,
  건당 처리 시간, 외부 전송 여부.

기준을 만족하지 못해도 그 자체가 결과다 — "지금 방식이 낫다"도 유효한 PoC
결론이다.

## 실행 위치·라이선스 제약 (점수와 별도로 먼저 거른다)

| 조건 | 판단 |
|---|---|
| 자료 외부 전송 가능 여부 | 지금은 공개 커뮤니티 글이라 제약 없음. 다만 "외부로 안 보내도 되는 방법이 있다면 그쪽이 낫다"가 이 PoC의 전제 |
| 후보 모델 라이선스 | `beomi/KcELECTRA-base-v2022` — **MIT**, 상업적 사용 가능 (HuggingFace 모델 카드 확인, 2026-09-22) |
| 학습 데이터 성격 | 네이버 뉴스 댓글(구어체·신조어·오탈자 포함) 1.8억 문장 — 위키/뉴스 본문이 아니라 노이즈 있는 커뮤니티 텍스트로 사전학습됨. 크립토 은어까지 커버하진 않지만 일반 LLM보다 출발선이 가깝다 |
| 언어 범위 | **한국어 전용으로 범위를 좁힌다.** 4chan/Reddit(영어) 글은 이번 PoC 대상에서 제외 — 한국어 사전학습 모델이 애초에 영어를 처리하도록 만들어지지 않았고, 실제 매매 신호에서 비중이 큰 소스도 디시·코인판이다 |

## 후보 (셋)

| 후보 | 실행 위치 | 라이선스/비용 | 비고 |
|---|---|---|---|
| 1. 지금 쓰는 방식 — Gemini 2.5 Flash | 구글 클라우드(외부) | 종량제, 상업 이용 허용 | 기준선 |
| 2. 규칙 기반 키워드 매칭 (이미 코드에 있는 `MockClassifier`) | 로컬 | 자체 코드, 비용 0 | 하한선 참고용 |
| 3. KcELECTRA-base-v2022 파인튜닝 | 로컬 CPU | MIT, 비용 0 | 이번 PoC의 실험 대상 |

## 검증 방법

정량 — 20건 검증셋에 대해 세 후보 모두 같은 입력으로 예측을 돌려
정확도·혼동행렬·오분류 사례를 [`poc/evaluate.py`](poc/evaluate.py) 로 비교한다.
결과는 [`results/`](results/) 에 남긴다.
