# 문제 정의서 — 커뮤니티 글 감성분류, LLM 외부 API 탈출

작성일: 2026-09-22

## 도메인 설명

비공개 저장소로 운영 중인 파이프라인이 하나 있다. 디시인사이드·코인판·
레딧·4chan 에서 암호화폐 관련 커뮤니티 글을 실시간으로 수집해 감성
(POSITIVE/NEGATIVE/NEUTRAL)을 분류하고, 그 분류 결과를 다운스트림 집계
로직의 입력으로 쓴다.

지금까지 쌓인 원문 글은 **211,513건**이다(2026-09-22 기준, 운영 피드 화면
실측). 그중 감성 라벨이 붙은 글은 **126,506건**인데 이 값은 운영이 아니라
**로컬 개발 DB 기준이고 운영 기준 값은 확인하지 않았다** - 실제로는 더 많다.
글은 제목만 있고 본문은 항상 비어 있다(수집 단계 설계상 크롤러가 본문을
수집하지 않는다).

> **[2026-09-22 정정]** 원문 글 건수는 원래 *"원본 DB에 **167,801건**"* 이라고
> 적혀 있었다. 이 값은 **운영이 아니라 로컬 개발 DB 를 잰 것**이라 같은 날짜
> 운영 실제치(211,513건)보다 43,712건, 약 26% 적었다. "원본 DB" 라고 단언해
> 놓고 로컬 사본을 쟀던 것이라 정정한다. 커밋 `64fdf41` 의 메시지 본문에도
> 로컬 기준 `126,506건` 이 그대로 들어가 있다.
>
> 이 숫자들은 도메인 규모를 설명하는 맥락일 뿐이고, 실제 실험이 쓴 것은
> `poc/data/eval_set.csv`(20건)와 `poc/data/silver_labels.csv`(2,676건)로
> 저장소에 스냅샷이 들어 있다. 따라서 **비교 결과 자체는 이 정정에
> 영향받지 않는다.**

## 현재의 문제

지금 분류를 맡고 있는 것은 **Google Gemini 2.5 Flash API**다. 시스템
프롬프트에 "암호화폐 커뮤니티 글의 감성분류"를 지시하고, 구조화 출력
스키마로 `{label, confidence, intensity, is_sarcasm}` 를 강제해서 받는다.

이 방식의 문제는 셋이다.

1. **자료가 외부로 나간다.** 글 원문(제목)이 매 건 구글 서버로 전송된다.
   지금은 익명 커뮤니티 공개 글이라 개인정보 문제는 크지 않지만, 뉴스 채널이나
   비공개 소스가 추가되면 이 경로가 바로 걸린다.
2. **건당 과금이다.** 분류 잡이 하루 수십~수백 회 돈다(잡 로그 기준
   최근 관측치 2,030회). 분류 대상이 늘수록 비용이 선형으로 늘고, API
   쿼터·요금 정책 변경에 그대로 노출된다.
3. **오프라인에서 못 돈다.** 네트워크·API 키·요금제 중 하나라도 막히면
   분류 파이프라인 전체가 멈춘다. 코드에 이미 키워드 매칭 폴백이 있다는
   사실 자체가, 이 리스크를 저자가 이미 알고 있었다는 뜻이다.

## 개선 가설

이 업무는 강의 2강의 **"분류와 배분"** 유형이다. 병목은 판단 자체가 아니라
판단의 총량(하루 수천 건)이다. 판단 기준(POSITIVE/NEGATIVE/NEUTRAL 세 갈래)은
이미 프롬프트로 명문화되어 있고, 12만 건이 넘는 과거 판정 기록(Gemini 라벨)이
있어 채점 자료를 만들 수 있으며, 오분류가 나도 사람 목숨이나 법적 효력에
영향을 주지 않고 되돌릴 수 있는 형태로 운용 중이다. 발생량도 충분하다.
강의 6강의 네 조건(판정 기준·채점 자료·되돌릴 수 있음·발생량)을 모두
만족하는 지점이다.

가설: **한국어 커뮤니티 특유의 구어체·은어(가즈아, 한강, 설거지, 개추 등)로
사전학습된 도메인 특화 모델을 우리 데이터로 파인튜닝하면, 이 3-분류 문제에서
Gemini API와 견줄 만한 정확도를 로컬·무료·오프라인으로 낼 수 있다.**
범용 LLM을 그대로 쓰는 대신, 강의 5강의 파인튜닝 기법(사전학습된 모델의
앞쪽 층은 얼리고 뒤쪽 판단층만 우리 데이터로 다시 학습)을 적용한다.

## 대상 사용자

이 분류 결과를 그대로 다음 단계 판단에 쓰는 하위 파이프라인(자동화된 집계
잡)이다 — 사람이 화면으로 결과를 보고 넘기는 구조가 아니라, 분류가 끝나면
바로 다음 자동화 단계가 그 값을 소비한다.

## 성공 기준 (모델을 돌리기 전에 확정)

한국어 게시글(디시인사이드·코인판) 20건으로 검증셋을 만들고, 모델을 돌리기
전에 **참조 라벨을 미리 확정**했다 ([`poc/data/eval_set.csv`](poc/data/eval_set.csv),
20건 중 8건은 반어법·정보 부족 등으로 Gemini와 판단이 갈리거나 애매한 "어려운"
사례로 골랐다).

> **[2026-09-22 정정]** 이 문단은 원래 *"**사람이** Gemini 라벨을 참고는 하되
> 최종 판단은 **직접 다시 내려** 정답을 미리 확정했다"* 고 적혀 있었다.
> **사실이 아니어서 정정한다.** `reference_label` 칸은 사람이 아니라 **Claude 가
> Gemini 라벨을 본 상태에서 매긴 것이고, 사람 검수를 거치지 않았다.** 커밋
> `64fdf41` 의 트레일러 기준으로 **Claude Sonnet 5** 이고, 노력(effort) 설정은
> 어디에도 기록이 없다. 이 칸의 원래 이름은 `human_label` 이었는데, 실제와
> 맞지 않아 `reference_label` 로 바꿨다.
>
> 따라서 이 PoC 가 재는 것은 사람 기준 정확도가 아니라 **LLM 참조 라벨과의
> 일치율**이다. 아래 성공 기준도 그 전제에서 읽어야 하고, 이것이 결과 해석에
> 미치는 영향은 [ANALYSIS.md](ANALYSIS.md) 의 한계 절에 정리했다. 사전 등록
> 문서를 사후에 고치는 것이므로 원문을 지우지 않고 남긴다.

- **주 기준**: 이 20건에서 참조 라벨과 **14건(70%) 이상** 일치하면 로컬
  대체 후보로 채택할 가치가 있다고 본다. (참고: 아무 판단도 안 하고 전부
  NEUTRAL로 찍는 기준선은 20건 중 7건 = 35% 다.)
- **치명 오류 제한**: POSITIVE↔NEGATIVE 로 방향이 정반대로 뒤집히는
  오류가 **2건을 넘으면 기준을 만족해도 채택하지 않는다.** 다운스트림이
  이 분류 결과를 이진적 판단(가도록/멈추도록 등)의 근거로 바로 쓰므로,
  방향이 뒤집히는 오류가 NEUTRAL 오분류보다 훨씬 비싸다.
- **참고 지표(합격/불합격을 가르지는 않음)**: Gemini API 대비 일치율 차이,
  건당 처리 시간, 외부 전송 여부.

기준을 만족하지 못해도 그 자체가 결과다 — "지금 방식이 낫다"도 유효한 PoC
결론이다.

## 실행 위치·라이선스 제약 (점수와 별도로 먼저 거른다)

| 조건 | 판단 |
|---|---|
| 자료 외부 전송 가능 여부 | 지금은 공개 커뮤니티 글이라 제약 없음. 다만 "외부로 안 보내도 되는 방법이 있다면 그쪽이 낫다"가 이 PoC의 전제 |
| 후보 모델 라이선스 | `beomi/KcELECTRA-base-v2022` — **MIT**, 상업적 사용 가능 (HuggingFace 모델 카드 확인, 2026-09-22). 2차 추가분은 아래 후보 표 참고 — **둘은 라이선스가 명시돼 있지 않다** |
| 학습 데이터 성격 | 네이버 뉴스 댓글(구어체·신조어·오탈자 포함) 1.8억 문장 — 위키/뉴스 본문이 아니라 노이즈 있는 커뮤니티 텍스트로 사전학습됨. 크립토 은어까지 커버하진 않지만 일반 LLM보다 출발선이 가깝다 |
| 언어 범위 | **한국어 전용으로 범위를 좁힌다.** 4chan/Reddit(영어) 글은 이번 PoC 대상에서 제외 — 한국어 사전학습 모델이 애초에 영어를 처리하도록 만들어지지 않았고, 실제 수집량에서 비중이 큰 소스도 디시·코인판이다 |

## 후보

### 1차 — 모델을 돌리기 전에 사전 등록한 셋

| 후보 | 실행 위치 | 라이선스/비용 | 비고 |
|---|---|---|---|
| 1. 지금 쓰는 방식 — Gemini 2.5 Flash | 구글 클라우드(외부) | 종량제, 상업 이용 허용 | 기준선 |
| 2. 규칙 기반 키워드 매칭 (이미 코드에 있는 `MockClassifier`) | 로컬 | 자체 코드, 비용 0 | 하한선 참고용 |
| 3. KcELECTRA-base-v2022 파인튜닝 | 로컬 CPU | MIT, 비용 0 | 이번 PoC의 실험 대상 |

### 2차 — 1차 결과를 본 뒤 추가한 셋 (사전 등록 아님)

> **[2026-09-22 추가]** 1차에서 KcELECTRA 가 일치율은 Gemini 에 근접(60% vs
> 65%)하는데 방향 반전만 3건 났다. 이것이 **파인튜닝 방식의 한계**인지 **그
> 모델 말뭉치의 한계**인지 가리려고, 문체·도메인이 다른 사전학습 모델 셋에
> 같은 학습·평가를 돌렸다.
>
> **이 셋은 1차 결과를 보고 고른 것이므로 사전 등록 후보가 아니다.** 검증셋과
> 성공 기준은 1차와 동일하지만, 결과를 본 뒤 후보를 늘리면 유리한 것이 걸릴
> 확률이 올라간다. 1차와 같은 무게로 읽으면 안 된다.

| 후보 | 실행 위치 | 라이선스/비용 | 사전학습 말뭉치 |
|---|---|---|---|
| 4. klue/roberta-base 파인튜닝 | 로컬 CPU | **라이선스 미확인**, 비용 0 | 위키·뉴스 (격식체) |
| 5. KoELECTRA-v3 파인튜닝 | 로컬 CPU | Apache-2.0, 비용 0 | 뉴스·위키·블로그 (격식체) |
| 6. KR-FinBert-SC 파인튜닝 | 로컬 CPU | **라이선스 미확인**, 비용 0 | 경제뉴스·증권사 리포트 (금융 도메인) |

4번·6번은 모델 카드에 라이선스 표기가 없다(2026-09-22, HuggingFace README·
태그 확인). 위 제약 표의 기준대로라면 **상용에는 성능과 무관하게 거른다.**
이번 PoC 는 개인 실험이라 비교 목적으로만 돌렸다.

## 검증 방법

정량 — 20건 검증셋에 대해 모든 후보에 같은 입력으로 예측을 돌려
일치율·혼동행렬·오분류 사례를 [`poc/evaluate.py`](poc/evaluate.py) 로 비교한다.
결과는 [`results/`](results/) 에 남긴다.
